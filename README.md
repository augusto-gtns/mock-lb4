# NodeJS backend with Loopback framework

http://localhost:3000/

## Run with Node

    npm start

## Run with Docker

Build docker image

    docker build . --tag mock-site-image

Run docker container

    docker-compose up --detach
